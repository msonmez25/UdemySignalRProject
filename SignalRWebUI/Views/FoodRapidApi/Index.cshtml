@model List<ResultTastyApiDto>
@{
    ViewData["Title"] = "Index";
    Layout = null;
}

<!DOCTYPE html>
<html>

@await Component.InvokeAsync("_UILayoutHeadComponentPartial")

<body class="sub_page">

    @await Component.InvokeAsync("_UILayoutNavbarComponentPartial")
    </div>


    <!-- Rapid Api List Inside Start  -->

    <div class="container-fluid">
        <div class="row pt-xxl-5 mt-5">
            <div class="col-12">
                <nav class="breadcrumb bg-dark mb-30">
                    <a class="breadcrumb-item text-white" href="/Default/Index/">Ana Sayfa</a>
                    <a class="breadcrumb-item text-white" href="/AboutHome/Index/">Hakkımızda</a>
                    <span class="breadcrumb-item active">Rapid - Tasty Api</span>
                </nav>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row pt-xxl-5">
            <div class="col-lg-12 table-responsive">
                <table class="table table-light table-borderless table-hover text-center mb-0">
                    <thead class="thead-dark">
                        <tr>
                            <th>#</th>
                            <th>Ürün Adı</th>
                            <th>Tahmini Süre</th>
                            <th>Tarifi İzle</th>
                        </tr>
                    </thead>   
                    @foreach(var item in Model)
                    {
                        <tr>
                            <td><img src="@item.thumbnail_url" style="width:50px;height:50px"/></td>
                            <td>@item.Name</td>
                            @{
                                if (@item.total_time_minutes==0)
                                {
                                    <td>15 dk</td>
                                }
                                else
                                {
                                    <td>@item.total_time_minutes dk</td>
                                }
                            }                           
                            <td><a href="@item.original_video_url" target="_blank" class="btn btn-warning">Tarifi İzle</a></td>
                        </tr>
                    }
                </table>
            </div>
            
        </div>
    </div>

    <!-- Rapid Api List Inside End -->

    @await Component.InvokeAsync("_UILayoutFooterComponentPartial")

    @await Component.InvokeAsync("_UILayoutScriptComponentPartial")




</body>

</html>

